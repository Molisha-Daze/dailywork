# P3 执行 + 发版 1.2.3 打包记录

> 执行时间：2026-09-22 00:00 – 01:35
> commit `c028632`；APK：`android/app/build/outputs/apk/release/app-release.apk`
> 状态：**P3 完成，1.2.3 已签名打包，验收通过**

---

## 一、P3-C 拆 `CalendarMonthView`（839 行 / 主函数 398 行）

### 拆分后的结构

`CalendarMonthView()` 从 398 行降到 **62 行**，只保留两件事：**状态宿主 + 两个卡片的组装**。

```
CalendarMonthView.kt (161)  ← 入口：currentMonth / selectedDate 两个 remember 只在这里
   ├─ CalendarCard
   │    ├─ MonthNavRow        月头：标题 / 「今日」跳转 / 上一月 / 下一月
   │    ├─ WeekdayHeaderRow   星期表头（周末琥珀色）
   │    ├─ MonthGridRows      整周对齐的网格
   │    │    └─ DayCell       RowScope 扩展：数字 + 状态点
   │    └─ CalendarFooterRow  图例 + 进入当天详情
   └─ SelectedDayPlanCard
        ├─ SelectedDayHeaderRow   标题 / N 项 / 新建 / 完整详情
        ├─ SelectedDayEmptyHint   当天无排期
        └─ CalendarDayPlanRow     单行计划（原本就有，318 行）

CalendarMonthGrid.kt (336)      ← 上半屏：月头 / 表头 / 网格 / 图例
CalendarDayPlanList.kt (557)    ← 下半屏：当天计划清单
```

| 指标 | 拆分前 | 拆分后 |
|---|---|---|
| 最大单文件 | **839** | **557**（`CalendarDayPlanList.kt`） |
| `CalendarMonthView()` 主函数 | 398 行 | **62 行** |
| 三件套合计 | 839 | 1,054（拆分 + 每份文件自己的 import/KDoc，**行数微增是预期内的**） |

### 两个必须遵守的坑（写进代码注释了）

1. **状态宿主不能下沉。`currentMonth` / `selectedDate` 的 `remember` 必须留在 `CalendarMonthView()`。**
   如果把它们挪进某个小块，翻月时该块会连同状态一起重建 —— 选中日期直接丢失。这也是我在拆分说明里明文写下的那条约定。
2. **`Modifier.weight` 只存在于 `RowScope` / `ColumnScope`，没有顶层扩展。**
   原代码里 `Box(Modifier.weight(1f))` 是写在 `Row { }` 的 lambda 里、靠 RowScope 隐式接收者生效的；
   一提取成独立的 `DayCell` 就编译不过。**解法是写成 `private fun RowScope.DayCell(...)`**，这也是 Compose 的惯用写法。
   （我先试了 `import androidx.compose.foundation.layout.weight`，编译器直接报「it is internal in file」——记下来，别再踩。）

另外顺手清掉一个死常量：`CAL_PURPLE` 在 `CalendarMonthView.kt` 里定义后**从未被使用**（它和 `CAL_AMBER` 一起声明，但只有 AMBER 用到了）。

---

## 二、P3-B 文件归属整理

| 改动 | 说明 |
|---|---|
| `ui/screens/HabitsManageScreen.kt` → **`ui/components/HabitManageSection.kt`** | 它唯一对外符号是 `HabitManageSection`，被 `SettingsScreen:144` 内嵌，**根本不是一个页面**。同时把 package 改成 `ui.components` |
| `ui/theme/Theme.kt`（27 行）→ **并入 `AppTheme.kt`** | 里面只有 `HabitTrackerTheme`，和 `AppTheme` 名字太像、检索时反复走错 |
| `model/HabitModels.kt` + `model/SubTask.kt` → **`model/Models.kt`** | 两个文件三个 data class，合成一个 |
| `EmptyState.kt` | **不动**。报告说「被 2 处使用、可并入邻文件」，实测是 **3 处**（今日 / 计数器 / 计划清单）——57 行三处复用，是这套代码里少见的优秀抽象，该保留 |

---

## 三、P3-F 注释收敛

「`#10B981` 同时是计划色板第一支，写死它会导致撞色」这段故事原本在 4 个文件里各写一整段。
现在统一收敛成一行指针，权威叙事只留在 `AppTheme.kt` 顶部的「设计硬约束」注释块：

| 文件 | 处理 |
|---|---|
| `HabitCard.kt` | 5 行 → 2 行指针 |
| `DayDetailDialog.kt` | 4 行 KDoc → 1 行 |
| `TodayScreen.kt` | 3 行 → 2 行指针 |
| `CalendarMonthView.kt`（`calAccent`） | 6 行 → 保留「为什么放这里」+ 1 行指针 |

按我上次核验时定的原则：**只收重复叙事，单点说明一个字没删。**

---

## 四、版本号 +0.0.2

| 项 | 旧 | 新 |
|---|---|---|
| `versionName` | 1.2.1 | **1.2.3** |
| `versionCode` | 10201 | **10203**（`1*10000 + 2*100 + 3`，编码规则未变） |
| README 里的版本表 | 同步更新 | ✓ |

「关于」页走 `PackageManager` 自动读，无需改动。

---

## 五、验收

| 指标 | 结果 |
|---|---|
| `:app:assembleRelease` + `testDebugUnitTest` | **BUILD SUCCESSFUL**，且**零新增编译警告** |
| 单元测试（XML 解析） | **71 条 / 失败 0 / 错误 0 / 跳过 0** |
| `aapt dump badging` | `versionCode=10203` `versionName=1.2.3` `io.github.molishadaze.weijing` ✓ |
| `apksigner verify` | 签名证书 `CN=Weijing`，SHA-256 `1bb71eeb…a211f` **与历史一致**（可平滑升级） |
| APK 体积 | **21,261,536 B（20.28 MiB）** |
| dex 内 `androidx.navigation` | **0**（P1 的成果保持住了） |
| dex 内 `CalendarHeatMap` | 不存在 |
| main 源码 | 50 文件 / 11,013 行（P1 后是 10,807；拆分带来的 import 与 KDoc 让它微增，符合预期） |

---

## 六、⚠️ 本次改动的验证边界（必须知道）

**拆分和改名全部集中在 UI 层，而我只有「编译通过 + 71 条逻辑单测」这两道保险 —— 没有任何一条自动测试能覆盖到「画得出来」。**

- 本机依旧**没有 UI 测试能力**（无模拟器、离线缓存里没有 `androidx.test` / `ui-test-junit4` / robolectric）；
- 上次给你留的 MuMu 安装包（`E:\anzhuo\mumu\MuMuInstaller.exe`）如果还没装，**强烈建议先装上再把 1.2.3 装进去过一遍**：
  历史页（月历翻月 / 点日期 / 今日跳转 / 图例 / 当天清单 / 新建 / 完整详情）是这次改动的唯一受影响面，
  还有一个顺带受影响的页面是管理中心的计划清单（`HabitManageSection` 换了文件与包名）。
- 我的静态核对：拆分后所有 lambda 参数逐一比对，状态宿主、选中态夹取逻辑（`effectiveSelectedDate`）、
  「今日计划清单」标题的 `weight(1f, fill = false)`、未完成状态点的 `#9CA3AF` 都保持原样。

## 七、剩余

- **P2 未做**（D1 颜色三类治理 / D2 星期四合一 / D3 工具去重）——你这次跳过了它，不影响发版。
- P3 里 `AddEditHabitDialog.kt`（822）、`SettingsScreen.kt`（725）、`StandaloneCountersScreen.kt`（691）
  仍是最大的三个文件，下次要拆时建议照这次的 **`RowScope.weight` + 状态宿主不上移** 两条规矩来。
