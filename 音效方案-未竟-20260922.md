# 「未竟」音效方案

> **✅ 已于 2026-09-22 实施完毕，随 v1.3.0（versionCode 10300）发布。**
> 实际落地与本方案的偏差见文末「§10 实施记录」—— 其中 DAY_DONE 的触发方式**改了**，动手前请先读那一节。
>
> 日期：2026-09-22　目标版本：v1.3.0（versionCode 10300）
> 前置：触觉反馈（`util/Haptics.kt`）已落地，音效与其共用同一套设计哲学与调用点。

---

## 0. 结论摘要

| 项 | 结论 |
|---|---|
| 加多少个音效 | **9 个**，其中 7 个默认开启、2 个默认关闭 |
| 音效从哪来 | **本地合成**（已产出 `android/gen_sfx.py` + 9 个试听样本），原创 CC0，无版权负担 |
| 体积增加 | **385 KB**（WAV 44.1k/16bit/单声道）。总量 21.2MB → 21.6MB，占 1.8% |
| 技术载体 | 新增 `util/Sounds.kt`（镜像 `Haptics` 的 object 单例 + SoundPool） |
| 关键取舍 | 音频属性走 **音效流（SONIFICATION）** 而非媒体流 —— 静音/勿扰自动无声 |
| 不做的事 | 点击音、导航音、自定义音量滑块、通知音（已有通知渠道声音） |
| 需要你拍板 | 4 个决策点，见 §7 |

---

## 1. 设计原则：音效必须比触觉更保守

触觉和声音的**社交成本完全不同**。振动是私密的——手握着才有知觉，旁人听不见；
声音是公开的——在会议室、地铁、深夜床边，一声"叮"的代价远高于一次振动。

所以不能把 `Haptics` 的档位表直接照搬成音效表。三条铁律：

**① 只在「成就时刻」响，不做点击音。**
用户每点一下都有反馈是**触觉**的职责。音效的职责是标记"这件事值得被记住"。
每个操作都配声音的产品，用户第一反应是去设置里关掉它。
（顺带：如果系统开了"触摸音效"，点击本身已有系统"啵"声，我们再叠一层只会显得廉价。
我们的音效与系统点击音**不重叠**，因为响的时刻完全不同。）

**② 一次事件最多一个音，不做队列、不做延迟补播。**
禁止"因为刚才被节流了所以现在补响"。宁可少响，不可迟到——
迟到的音效会与当前事件错位，用户会认为界面对应错了。

**③ 绝不阻断主流程。**
音效是糖，打卡是饭。任何播放异常全部 try-catch 吞掉，
SoundPool 初始化失败、资源加载失败、ROM 策略拦截，都必须不影响打卡数据落库。

### 1.1 响度即层级

不靠"音量滑块"表达重要性，靠**峰值设计**。这样用户不需要调任何东西就能感知层级：

| 层级 | 峰值 | 代表音效 |
|---|---|---|
| 里程碑 | -0.9 dBFS | 今日全部完成 |
| 成就 | -1.3 ~ -2.2 dBFS | 打卡完成、计数达标 |
| 确认 | -3 ~ -5 dBFS | 清零、撤销 |
| 细节 | -7.5 dBFS | 勾选子任务 |

全部留 headroom，**无一归一化到满刻度**——峰值顶满的短音效在手机小扬声器上会削波，
听起来是"呲"而不是"叮"。

---

## 2. 音效清单

### 2.1 会响的（默认 7 个 + 可选 2 个）

| # | 文件 | 触发动作 | 音色设计 | 对应触觉 | 默认 |
|---|---|---|---|---|---|
| 1 | `sfx_achieve_habit` | 单个习惯**打卡完成** | 双音上行 C6→E6，90ms 间隔，FM 铃铛 | STRONG | ✅ 开 |
| 2 | `sfx_achieve_plan` | **计划内子任务全部勾满** | 三音琶音 C6-E6-G6，尾音加长 | STRONG | ✅ 开 |
| 3 | `sfx_achieve_day` | **今日排期全部完成** | 四音上行 C6-E6-G6-C7 + 低八度铺底，1.05s | STRONG | ✅ 开 |
| 4 | `sfx_counter_goal` | 计数器**恰好达标** | 高亮单音 A6（跳出于成就音音区之外） | STRONG | ✅ 开 |
| 5 | `sfx_counter_limit` | 独立计数器**到达上限** | 低音五度和弦 C5+G5，"满了"的收束感 | STRONG | ✅ 开 |
| 6 | `sfx_clear` | 计数器**清零** / 删除确认 | 310→150Hz 指数下滑，音量压到 0.58 | MEDIUM | ✅ 开 |
| 7 | `sfx_restore` | **备份恢复完成** | 低频宽音程上行 C5→G5 | 无 | ✅ 开 |
| 8 | `sfx_undo` | 取消打卡 / 撤销 | 下行两音 G5→D5 | LIGHT | ❌ 关 |
| 9 | `sfx_step_soft` | 勾选子任务 / 计数器 ±1 / 表单保存 | 60ms 木质轻点（噪声×正弦） | LIGHT | ❌ 关 |

**为什么 `sfx_undo` 默认关**：撤销是"退回"。给它配音效会变成一种微妙的奖励，
与"取消不该有正反馈"的产品逻辑冲突。留给明确需要听觉确认的用户自己开。

**为什么 `sfx_step_soft` 默认关**：计数器连点十下就是十声"嗒"。
短时间内重复的短音会被大脑归类为**噪音**而不是反馈——这是听觉的基本机制
（听觉适应），不是调小音量能解决的。要做成默认开启必须配合极激进的节流，得不偿失。

### 2.2 坚决不响的

| 动作 | 为什么 |
|---|---|
| 切换 4 个 tab / 翻月 / 选日期 | 导航类无声。这是移动端的默认预期 |
| FAB 展开、弹窗开合、弹窗关闭 | 同上 |
| 勾选单个子任务、计数器未达标的 ±1 | 高频重复，见上 |
| 取消打卡 | 撤销不该有奖励感 |
| 每日格言刷新 | 后台行为，且响应式触发的时机不固定 |
| 提醒通知到达 | **通知渠道本身已有声音**，再叠一层就是双响 |
| 通知栏「标记完成」打卡 | 后台路径 —— 用户可能正在开会/在别的 app 里，突然出声极其突兀。**振动保留，音效拦截** |
| App 冷启动 / 进入主页 | 打断用户，且没有语义价值 |

### 2.3 事件冲突处理（重要）

同一次用户操作可能同时满足多个条件。规则：**只播等级最高的那一个**。

| 场景 | 候选 | 实际播放 |
|---|---|---|
| 勾满最后一个子任务，且这是今天最后一项待办 | `achieve_plan` + `achieve_day` | **`achieve_day`** |
| 计数器 +1 后恰好等于目标值且等于上限 | `counter_goal` + `counter_limit` | **`counter_limit`** |
| 勾满最后一个子任务，但今天还有别的计划 | `achieve_plan` | `achieve_plan` |

优先级：`achieve_day` > `achieve_plan` = `counter_limit` > `achieve_habit` = `counter_goal` > `clear` > `restore`。

### 2.4 音效设计的三条听觉规则

**① 音数编码等级。** 2 音 = 单个习惯，3 音 = 一组计划，4 音 = 全天。
用户听两三次就能建立"音数越多、成就越大"的直觉，**不用看屏幕就知道发生了什么**。

**② 全部落在 C 大调自然音。** 9 个音效共用一个音高体系，听起来才像同一个产品。
混用不同调式会出现游离的半音，整组音效立刻显得"拼凑"。

**③ 破坏性操作配不好听的音。** `sfx_clear` 用无音高感的下滑低音，音区低、音量压到 0.58。
给它配一个清脆的"叮"等于在鼓励用户多删数据。

---

## 3. 音效从哪来：三个方案对比

| | **A. 本地合成**（推荐） | B. CC0 素材库 | C. 系统按键音 |
|---|---|---|---|
| 载体 | `gen_sfx.py` 数值生成 | Kenney / Freesound / Mixkit 下载 | `AudioManager.playSoundEffect()` |
| 版权 | **原创，可声明 CC0，零溯源负担** | CC0 可商用，但需逐个核对许可与署名 | 免 |
| 音质上限 | 中上（合成器水平） | 高（专业录音/音源） | 中，且随 ROM 变 |
| 体积 | 385 KB | 200 KB ~ 1 MB+ | **0** |
| 风格一致性 | 高（同一套音高体系） | 低（多来源混用易割裂） | 无一致性可言 |
| 可调参 | ✅ 改数字重跑 | ❌ 只能换素材 | ❌ 完全不可控 |
| 离线可行 | ✅ 已跑通 | ❌ 需联网 | ✅ |
| 本机可行性 | **✅ 已验证** | ⚠️ 无 ffmpeg / 有网络时可下 | ✅ |

### 推荐 A，理由

1. **已跑通。** `android/gen_sfx.py` 已生成 9 个样本（`android/sfx-preview/`），
   波形核验：零削波、首尾样本精确为 0、峰值层级 -0.9 ~ -7.5 dBFS。
2. **本机没有 ffmpeg、没有 numpy。** 脚本只用标准库 `wave`/`math`/`array`，
   在任何有 Python 的机器上都能重跑（本机用 `C:\Users\admin\.workbuddy\binaries\python\versions\3.13.12\python.exe`）。
3. **可调参是长期价值。** 觉得"太尖"就降 `fm_index`，觉得"太长"就降 `tau_ms`，重跑 30 秒。
   外购素材只有"换一个"这一个自由度。
4. **与项目习惯一致。** 启动图标已经用 `android/gen_icons.py` 走生成脚本路线，
   音效沿用同一模式，工程里只有一个"资源是生成的"这一处认知负担。

### 备选路径（保留架构不变）

若对合成音色不满意，可**只替换素材、不动代码**：把 CC0 素材裁到 ≤1s、
统一做峰值归一化，按 `sfx_*.wav` 命名塞进 `res/raw/` 即可。
`Sounds.kt` 只认 `R.raw.sfx_*` 这个名字，不关心文件怎么来的。

### 合成配方的技术要点（已实现在脚本内）

- **FM 铃铛**：载波相位被 3× 频率的调制波推动，调制指数随 45ms 快速衰减。
  结果是"起音是金属叮、尾部回落到纯净正弦"——这正是真实铃铛的行为。
  比叠加整数泛音好在哪：整数泛音太"电子琴"，FM 的分数比旁瓣会让耳朵判定"这是打击乐器"。
- **首尾强制淡入淡出**（2ms / 4ms）。波形从 0 突跳到非零值就是一次宽带脉冲，
  听起来是一声"咔"，与音效本身无关，纯属工程噪声。这是最常见的自建音效事故。
- **尾部裁剪到 -58dB**。指数衰减的尾巴很长，但手机扬声器上 -58dB 以下听不见，裁掉省 10~20% 体积。
- **指数滑音用相位累加**而不是直接代入 `sin(2πft)`——频率在变时直接代入会算错相位，产生跳变杂音。

---

## 4. 技术实现

### 4.1 新增 `util/Sounds.kt`

完全镜像 `Haptics` 的架构（object 单例 + `attach()` + 安全 no-op）：

```kotlin
object Sounds {
    enum class Sfx(@RawRes val resId: Int): 
        ACHIEVE_HABIT(R.raw.sfx_achieve_habit), ACHIEVE_PLAN(...), ... 

    fun attach(context: Context)     // HabitApplication.onCreate 调用一次
    fun play(sfx: Sfx)               // 检查全部开关 + 节流 + 前台
    fun preview(sfx: Sfx)            // 试听：绕过开关与节流，仅设置页用
    fun isSystemVolumeSilent(): Boolean   // 设置页诊断用
    fun release()                    // 目前不需要调用
}
```

**为什么是 object 而不是 Composable 参数**：与 `Haptics` 同因——
`CheckInActionReceiver`（通知栏路径）也要能调用它（虽然那个路径最终会被前台判断拦截）。

### 4.2 音频属性：走音效流而不是媒体流

这是**最需要拍板的取舍**，两条路都有代价：

| | 音效流 `USAGE_ASSISTANCE_SONIFICATION` | 媒体流 `USAGE_MEDIA` |
|---|---|---|
| 映射到 | 系统音量（STREAM_SYSTEM） | 媒体音量（STREAM_MUSIC） |
| 手机静音/勿扰时 | **自动无声** ✅ | 仍然会响 ❌ |
| 用户听歌时 | 不叠加进音乐 ✅ | 混在音乐里 ❌ |
| 系统音量常年为 0 的用户 | **完全听不见** ❌ | 能听见 ✅ |
| 符合语义 | 键盘点击声、系统提示 | 音乐、视频 |

**推荐音效流。** 理由：手机"静音模式"只静铃声+通知，媒体音量不变。
如果走媒体流，用户在会议室按静音键后打卡，依然会响一声——这是**不可接受的失误**。
宁可"系统音量低时听不见"，也不要"该静的时候响了"。

代价的缓解措施（必须一起做，否则会变成"装了没声音"的差评）：
- 设置页**实时检测系统音量**，为 0 时给出明确提示，见 §5
- 提供**试听按钮**，让用户当场判断能不能听见
- 设置项副标题写明"跟随系统音量，静音模式下不会播放"

### 4.3 前台判断：绝不在后台出声

`CheckInActionReceiver` 在后台运行（通知栏「标记完成」）。它调 `Haptics.play(STRONG)` 是对的
（振动私密），但音效在那个场景出声是错的——用户可能正在开会，或在别的 app 里。

**实现（零依赖）**：`attach()` 时注册 `Application.ActivityLifecycleCallbacks`，
用 `onActivityStarted` / `onActivityStopped` 维护一个可见 Activity 计数，`play()` 时判定计数 > 0。

不引入 `lifecycle-process` 依赖——`ProcessLifecycleOwner` 在那个 artifact 里，而本项目
**离线无新依赖**。framework 的 `ActivityLifecycleCallbacks` 够用，20 行代码。

### 4.4 节流：150ms，分档独立计时

`Haptics` 用 70ms。音效必须更长（150ms），原因：
- 振动只有 12~32ms，连点不会叠加；
- 音效有**尾音**（最长的 1.05s），两次播放会重叠成噪音。

**必须分档独立计时**（照搬 Haptics 的做法）：否则"连点计数器（`step_soft` 被节流）"
会把紧接着"刚好达标（`counter_goal`）"的那一声一起吞掉。
这是 Haptics 已经踩过并解决的坑，音效直接复用结论。

### 4.5 SoundPool 的四个坑

**坑 1：`load()` 是异步的，首次 `play()` 可能无声。**
必须 `setOnLoadCompleteListener` 记录就绪状态，未就绪时 `play()` 直接返回。
预热时机：`MainActivity` 首个 Compose 帧之后异步预热（`LaunchedEffect` + `Dispatchers.IO`），
**不要放在 `Application.onCreate`** —— 会给冷启动加时间。

**坑 2：`maxStreams` 要小。** 设 3~4。设大了不会更好，只会让意外重叠的杂音更响。

**坑 3：音频属性必须在构造 SoundPool 时指定**，`play()` 阶段改不了。

**坑 4：加载失败返回 0，`play(0)` 是静默无效的。**
必须判空，且不能因此崩溃——整个 `play()` 包在 try-catch 里。

### 4.6 调用点改造：15 处，建议引入 `Feedback` 门面

现有 `Haptics.play(...)` 调用点（行号为 1.2.4 快照）：

| 文件 | 行 | 现有语义 | 需要补的音效 |
|---|---|---|---|
| `HabitCard.kt` | 339 | 勾选主复选框完成/取消 | `achieve_habit`（取消不响） |
| `HabitCard.kt` | 294 | 子任务达标 | `achieve_plan` |
| `HabitCard.kt` | 264 | 单个子任务勾选 | `step_soft`（默认关） |
| `HabitCard.kt` | 561 | `toggleWithHaptic` | `achieve_plan` / `achieve_day` |
| `CalendarDayPlanList.kt` | 263 / 363 / 461 | 打卡 / 计数达标 / 计划完成 | 同上三者 |
| `DayDetailDialog.kt` | 254 / 339 / 407 | 同上 | 同上三者 |
| `StandaloneCountersScreen.kt` | 523 / 544 | 步进 / 到达上限 | `step_soft` / `counter_limit` |
| `StandaloneCountersScreen.kt` | 216 / 265 | 清零 / 删除确认 | `clear` |
| `SettingsScreen.kt` | 324 | 开关打开时预览 | `preview()` |
| `CheckInActionReceiver.kt` | 74 | 通知栏打卡（后台） | **不播**（前台判断自动拦截） |

**建议引入 `util/Feedback.kt` 门面**，把"触觉档位 + 音效 + 事件冲突优先级"封在一处：

```kotlin
object Feedback {
    enum class Event { HABIT_DONE, HABIT_UNDONE, SUBTASK_TOGGLED, PLAN_DONE, DAY_DONE,
                       COUNTER_STEP, COUNTER_GOAL, COUNTER_LIMIT, CLEARED, RESTORED, PREVIEW }
    fun fire(event: Event, planCompleted: Boolean = false, dayCompleted: Boolean = false)
}
```

好处：① 冲突优先级（§2.3）只写一遍，不会在 15 个调用点各写一份；
② 新增事件不用记着"这里还要加一句音效"；③ 音效整体开关只在一个地方判定。

代价：要改 8 个文件的 15 处调用。**建议做**——15 处分散调用意味着将来加第 10 个音效时，
你会漏掉某几处，而这种漏是静默的（不报错、不崩，只是没声音）。

### 4.7 新增触发点：「今日全部完成」（唯一一个现有代码没有的）

现有代码**没有**"今日排期全部完成"的触觉调用点，所以 `sfx_achieve_day` 需要新增触发。

**必须做边沿检测**，否则会出现"每次进今日页都播一遍"的 bug：

```
只在「完成率 从 <100% 变为 ==100%」的瞬间播放，且冷启动时首次值不触发。
```

用 `remember { mutableStateOf<Float?>(null) }` 记录上次完成率，
首次（`null`）只记录不播放。这是本方案最容易写错的一处。

---

## 5. 设置入口

### 5.1 位置：紧跟 `HapticFeedbackCard`，不进金刚区

管理中心金刚区是**固定 4 个**（视觉/关于/备份/提醒），与网页版一一对应，
塞第五个会让两端导航结构错位。所以音效开关做成**第二张独立卡片**，
紧挨着现有的 `HapticFeedbackCard`（`SettingsScreen.kt:141` 之后）。

### 5.2 卡片设计（复用 `HapticFeedbackCard` 的视觉语言）

```
┌──────────────────────────────────────────────────┐
│ 🔊  音效反馈                    [开关]            │
│     打卡完成、计数达标会有提示音                    │
└──────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────┐
│ 🔊  细节音效                    [开关]            │
│     勾选子任务、计数器步进也会有声音                 │
└──────────────────────────────────────────────────┘
│  [ 试听 ]   系统音量：中（可听见）                  │
```

副标题按状态三元分支，逻辑照搬 `HapticFeedbackCard` 的"三种没反应的原因必须区分开"：

| 状态 | 副标题 |
|---|---|
| 系统音量 = 0 | 「系统音量已静音，音效无法播放」+ 跳系统音量设置 |
| 音效流受勿扰模式限制 | 「勿扰模式开启中，音效会静音」 |
| 开启 | 「打卡完成、计数达标会有提示音」 |
| 关闭 | 「已关闭，所有操作都不会有声音」 |

**「试听」按钮 + 系统音量诊断**是这套设计的灵魂：因为是音效流，
"没声音"的原因有 4 种（App 开关关、系统音量 0、勿扰、媒体音量为 0 但音效流没调）。
没有诊断，用户只会得出"这功能是坏的"这一个结论。

### 5.3 `AppSettings` 新增

```kotlin
val soundEnabled: Flow<Boolean>        // 主开关，默认 true
fun isSoundEnabled(): Boolean          // 同步读，供 Sounds 使用
fun setSoundEnabled(enabled: Boolean)

val soundDetailEnabled: Flow<Boolean>  // 细节音效，默认 false
fun isSoundDetailEnabled(): Boolean
fun setSoundDetailEnabled(enabled: Boolean)
```

与 `hapticEnabled` 完全同构（同步 + Flow 双形态）。
`PREF_NAME` 与 `KEY_*` 沿用同一文件，让 `Sounds` 也能像 `Haptics` 那样直接监听。

**注意**：音效开关属于设备本地显示偏好，**不进备份文件**（与 `fontScale`/`themeId` 一致）。

---

## 6. 验证方案

### 6.1 编译期保护（最强的一道）

`Sounds.Sfx` 的每个枚举项都引用 `R.raw.sfx_*`。
**如果资源文件不存在，`R.raw` 里就没有那个字段 → 编译直接失败。**
这是比任何单测都硬的保证，前提是映射表用 `R.raw.xxx` 硬引用，
**绝不能**用字符串拼接 + 反射查资源 id。

### 6.2 单元测试：`SoundsTest`

| 用例 | 断言 |
|---|---|
| 映射表完整性 | 9 个 Sfx 的 resId 全部非 0 且**互不相同**（防复制粘贴改漏） |
| 默认开关 | 恰好 7 个默认开启、2 个默认关闭，且关闭的是 `UNDO` / `STEP_SOFT` |
| 冲突优先级 | `DAY_DONE + PLAN_DONE` → 只出 `DAY_DONE`；`GOAL + LIMIT` → 只出 `LIMIT` |
| 节流（纯函数抽出） | 同档 100ms 内第二次被拦；跨档不被拦（Haptics 同款逻辑） |
| 前台判定 | 可见计数 0 时 `play()` 不产生播放调用（用可注入的播放器接口） |
| 开关判定 | `soundEnabled=false` 时成就音被拦，但 `preview()` 仍可出声 |

节流与优先级逻辑必须写成**纯函数 / 可注入**的形式，否则 JVM 单测（无 Robolectric）
根本碰不到 `SoundPool`。

### 6.3 声音无法用截图验证（诚实说明）

「编译通过 ≠ 画得出来」这条项目铁律，在音效上是**更严重**的版本：
`Build SUCCESSFUL` 和"真的出声了"之间隔着 4 层（资源加载 → 音频流 → 系统音量 → 硬件）。

| 手段 | 能验证什么 | 不能验证什么 |
|---|---|---|
| 编译 | 资源文件存在、映射表有效 | 有没有声音 |
| 单测 | 开关/节流/优先级逻辑 | 有没有声音 |
| `MuMu` + `adb install` | 界面能跑、点击不崩、`dumpsys audio` 能看到流 | **听不见**（`screenrecord` 不录音频） |
| logcat 断言 | `play()` 被调到、resId 是多少、是否被节流拦下 | 实际听到的效果 |
| **人工在真机听** | 音色好不好听、音量合不合适 | — |

**可执行的验证流程**：
1. Gradle 构建 + 单测（照常规流程，用例数会从 79 → ~90）
2. MuMu 里 `adb install -r`，`adb logcat | grep Sounds` 确认 `play()` 被调到、没被节流误拦
3. `adb shell dumpsys audio` 确认播放流存在
4. **最后一步只能你自己听**——建议用 `android/sfx-preview/` 里的 9 个 WAV 先定音色，
   避免装到手机上才发现要改。

> 试听样本已生成：`android/sfx-preview/`（9 个 WAV，可直接双击播放）。
> 定稿后再 `python gen_sfx.py app/src/main/res/raw` 搬进资源目录。

### 6.4 真机必测的边界

- 系统静音模式 → 应该无声（这是设计目标，不是 bug）
- 勿扰模式「仅允许闹钟」→ 应该无声
- 连接耳机 → 应该从耳机出
- App 切后台后用通知栏打卡 → **不应该有声**，但应该有振动
- 连点计数器 10 次 → 若开了细节音效，应该是"嗒嗒嗒"而不是机关枪
- 冷启动进今日页，当天已完成 → **不应该响起** `achieve_day`

---

## 7. 需要你拍板的 4 个决策点

| # | 决策 | 选项 | 我的建议 |
|---|---|---|---|
| 1 | **音频流** | 音效流（静音自动无声，但系统音量 0 时听不见） / 媒体流（一定能听见，但静音模式下仍会响） | **音效流**。宁可少响，不可在该静的时候响 |
| 2 | **是否引入 `Feedback` 门面** | 引入（改 8 个文件 15 处，冲突优先级只写一遍） / 不引入（`Sounds` 与 `Haptics` 并列调用，改动小） | **引入**。15 处分散调用的漏是静默的，将来必踩 |
| 3 | **采样率** | 44.1kHz（385KB） / 32kHz（约 280KB，Nyquist 16kHz 对铃铛类音色无损失） | **44.1kHz**。省那 100KB 不值得引入重采样疑虑 |
| 4 | **细节音效（`step_soft`）要不要做** | 做但默认关 / 直接不做 | **做但默认关**。代码成本几乎为 0，给需要的人一个开关 |

---

## 8. 发版清单（若采纳）

同步 5 处，沿用既有约定：

| 项 | 值 |
|---|---|
| `build.gradle.kts` versionCode | `10204` → **`10300`** |
| `build.gradle.kts` versionName | `1.2.4` → **`1.3.0`** |
| `README.md` | 版本表加一行 1.3.0（音效反馈） |
| `strings.xml` | 无需改（`app_name` 不变） |
| 通知渠道 description | 无需改（音效与通知渠道无关） |

**新增文件**：
- `app/src/main/java/.../util/Sounds.kt`
- `app/src/main/java/.../util/Feedback.kt`（若采纳决策 2）
- `app/src/main/res/raw/sfx_*.wav` × 9
- `app/src/test/java/.../SoundsTest.kt`
- `android/gen_sfx.py`（已创建）
- `android/sfx-preview/`（已创建，试听用，可 gitignore）

**无新依赖** —— SoundPool 是 framework API，`ActivityLifecycleCallbacks` 也是。离线可构建。

---

## 9. 实施顺序

1. 试听 `sfx-preview/` 的 9 个样本，定音色（可能要重跑脚本调参）
2. 定稿后 `python gen_sfx.py app/src/main/res/raw`（需新建 `res/raw/` 目录）
3. 写 `Sounds.kt` + `AppSettings` 两个开关 + `HabitApplication.attach`
4. 写 `SoundsTest`（先跑通测试，再改调用点）
5. （可选）写 `Feedback.kt`，改 15 处调用点
6. 新增「今日全部完成」边沿检测触发点
7. 写 `SoundFeedbackCard`（含试听 + 音量诊断）
8. Gradle 构建 + 单测 → MuMu 装包 → logcat 核验 → 真机听
9. 改 versionCode/versionName → 出签名 release APK

---

## 10. 实施记录（2026-09-22）

**已发布：v1.3.0 / versionCode 10300，APK 20.69 MB。**

### 10.1 拍板结果（§7 的四个决策点）

| # | 决策 | 采纳 |
|---|---|---|
| 1 | 音频流 | 音效流 `USAGE_ASSISTANCE_SONIFICATION` |
| 2 | `Feedback` 门面 | 引入 |
| 3 | 采样率 | 44.1kHz（385KB） |
| 4 | 细节音效 | 做，默认关 |

### 10.2 与方案的偏差（三处，都是改进）

**① `DAY_DONE` 放弃边沿检测，改用参数透传 —— 这一条改变了 §4.7 的结论。**

方案原计划在 `TodayScreen` 监听「完成率从 <100% 跨到 ==100%」。实施时改为：
`TodayScreen` 算 `completesDay = (completedCount + 1 == totalCount)`，经 `HabitCard`
透传到 `SubTaskSection`，由卡片在**点击当下**直接选 `DAY_DONE`。

原因：边沿检测天然滞后于点击，会让 `PLAN_DONE` 与 `DAY_DONE` 在几百毫秒内先后响两声叠成噪音；
而如果为了避重叠去抑制先响的那一声，声音又会与动作错位。唯一干净的解法是**提前知道**，
而这一点只有调用点做得到。

副作用：漏掉「在历史日历页给今天补最后一笔」这条路径。可接受 —— 那个场景用户不在今日页，
响一个「全天完成」的收尾音反而突兀。

**② 顺手补了「删除计数器」的反馈。** 方案只写了「计数器清零 / 删除确认」，
实施时发现删除那条路径原本连触觉都没有，一并补上了 `CLEARED`。

**③ 实际改动了 17 处调用点**（方案估的是 15 处），多出的两处是 `SubTaskSection` 的参数透传
与上面那条删除确认。

### 10.3 实测结果（MuMu 模拟器）

| 项 | 结果 |
|---|---|
| Gradle `assembleRelease` + `testDebugUnitTest` | ✅ 93 例零失败（79 → +14） |
| APK 版本 / 签名 | ✅ `10300` / `1.3.0` / 应用名「未竟」/ 正式证书 `CN=Weijing` |
| 资源打包 | ✅ 9 个 wav 全部进包（aapt2 混淆成 `res/xx.wav`，正常） |
| **音效真的出声** | ✅ logcat `play ACHIEVE_HABIT soundId=1 stream=1` |
| 主开关 | ✅ 关闭后 0 条播放日志 |
| 细节开关 | ✅ 关闭时 0 声，打开后 1 声 |
| `preview` 绕过开关 | ✅ 主开关关着点试听仍出声（设计如此） |
| 节流 | ✅ 同一 shell 连点 4 次只出 1 声 |
| 稳定性 | ✅ 无崩溃 |

> 为什么 `soundId` 和 `stream` 都要看：前者为 0 表示资源没加载成功，后者为 0 表示播放失败。
> 两者都非 0 才叫「真的出声」—— 这是 `Build SUCCESSFUL` 完全覆盖不到的层面。

**未覆盖**：后台不出声（通知栏打卡路径）没有实测，需要先构造出通知，成本高于收益；
但拦截逻辑就是 `visibleActivities > 0` 一个判断，风险很低。

### 10.4 一处已知技术债

设置页新卡片用了 `androidx.compose.ui.platform.LocalLifecycleOwner`，该 API 已 deprecated
（官方建议迁往 `lifecycle-runtime-compose`）。本项目**不引新依赖**，且 `MainScreen`
已有同样写法，故保持一致、接受编译警告。将来若引入该依赖，两处一起迁。

---

## 附：本次已产出的文件

| 文件 | 说明 |
|---|---|
| `android/gen_sfx.py` | 音效合成脚本，纯标准库。改参数重跑即可调音色 |
| `android/sfx-preview/sfx_*.wav` × 9 | 试听样本，385KB。波形已核验：零削波、首尾为 0 |
| `android/app/src/main/res/raw/sfx_*.wav` × 9 | 进包资源（由脚本生成，勿手改） |
| `util/Sounds.kt` | 播放引擎：SoundPool + 音频属性 + 节流 + 前台判断 |
| `util/Feedback.kt` | 反馈门面：事件 → (触觉, 音效) 的唯一入口 |
| `test/.../SoundsTest.kt` | 14 例守门测试 |
