node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Python 字节码缓存：android/ 下的 gen_icons.py / gen_sfx.py / release_gitee.py
# 一被 import 或编译就会生成，是纯产物，不该进版本库。
__pycache__/
*.pyc

# Gradle / Android 构建产物与缓存
# android/.gradle 是机器与 Gradle 版本相关的缓存（checksums、dependencies-accessors、
# .lock、.bin、.class、自动生成的 Java 源码），不同环境会持续改动，绝不能进仓库。
.gradle/
local.properties
.kotlin/
captures/
.externalNativeBuild/
.cxx/
*.iml
.idea/

# ===== 发布签名：绝不允许进仓库 =====
# release.keystore      —— 私钥本体。泄露 = 别人能冒名发布你的 App。
# keystore.properties   —— 里面是明文密码。
# 两者一旦推上远端就应视为已泄露，必须换新密钥（代价是所有老用户卸载重装）。
# 正常做法：离线备份两处（如密码管理器 + 移动硬盘），不要只放在这台电脑上。
*.keystore
*.jks
keystore.properties

# ===== Gitee 发版令牌：绝不允许进仓库 =====
# android/release_gitee.py 优先读环境变量 GITEE_TOKEN，其次读这个文件。
# 令牌带仓库写权限：泄露 = 别人能用你的名义发布「新版」，
# 而所有已装用户都会自动装上去 —— 这等于一次供应链攻击。
# （更好的做法是别落盘，直接用 `GITEE_TOKEN=xxx python release_gitee.py`。）
.gitee-token

# ===== GitHub 发版令牌：同上，绝不允许进仓库 =====
# android/release_github.py 优先读环境变量 GITHUB_TOKEN，其次读这个文件。
# 泄露后果与 Gitee 令牌完全相同（能以你的名义发版）。
.github-token

# 图标抓取与对照生成的临时工作区（HabitIcons.kt 已自带全部路径数据，可随时重建）
.iconwork/

# ===== AI 助手工作区：绝不允许进仓库 =====
# .workbuddy/ 是本机的工作目录，对「构建 App」毫无用处，但会暴露本机环境信息：
#   memory/   —— 工作日志与项目约定，正文里含本机绝对路径（C:\Users\...、E:\...）
#   tmp/      —— 临时 diff、调试截图、构建日志、一次性脚本
#   commitmsg.txt / last.txt —— 提交信息草稿
# 历史教训：2026-09-19 起曾误提交 21 个文件到公开仓库，2026-09-23 已从索引移除。
# 注意：本规则只拦「未跟踪」文件；已跟踪过的必须 git rm --cached 才脱离。
.workbuddy/

# 音效预览目录：与 app/src/main/res/raw/ 逐字节重复（cmp 验过 9/9 相同）。
# 音效唯一真源是 res/raw/，此目录只是早期生成的副本，不再纳入版本管理。
android/sfx-preview/
