# 《优化评估-未竟-20260921.md》可行性核验

> 核验人：凛 😼  核验时间：2026-09-21 23:10
> 核验方式：全仓库 grep + 自写 Python 分析脚本 + `git` 考古 + APK dex 字节解析，逐条对照报告结论
> 结论先行：**报告的事实层准确率很高（可直接采信），执行层的可行性评估不成立（不能直接照做）。**

---

## 〇、一句话结论

| 维度 | 判定 | 说明 |
|---|---|---|
| 事实与数据 | ✅ **可信** | 18 处硬编码色、1472 行注释、8799 行代码、各长函数行数——抽验全部精确吻合 |
| 「零风险 / 立即可做」 | ❌ **不成立** | 工作区有 26 个文件、+1907/−1191 行**未提交**，且报告自己列的验证手段在本机不可执行 |
| 收益估算 | ⚠️ **口径混淆** | P0「≈540 行」里 318 行是 md 文档；真实源码净减约 300 行 ≈ −3%，不是 −9% |
| 执行顺序 | ⚠️ **需重排** | 真正的 P0 是「先落一次提交」，不是删代码 |

---

## 一、数据层核对（抽取 12 项）

| 报告说法 | 实测 | 判定 |
|---|---|---|
| Kotlin 文件 58（main 51 + test 7） | 51 + 7 = 58 | ✓ |
| 总行数 12,343 | 11,060 + 1,283 = **12,343** | ✓ 精确 |
| `app/src/main` 11,111 行 | **11,060** | △ 差 51 = 文件数，是「是否计入末行」的计数口径差，非错 |
| 注释 1,472 行 | **1,472** | ✓ 精确 |
| 空行 840 行 | 789 | △ 口径差（是否计入纯空白缩进行） |
| 实际代码 ≈8,799 | **8,799** | ✓ 精确 |
| 超过 700 行的文件 4 个，最大 840 | 839 / 822 / 724 / 709，最大 CalendarMonthView **839** | ✓ |
| 非色板处 `Color(0xFF…)` 18 处散落 8 文件 | **18 处，8 文件** | ✓ 精确 |
| `strings.xml` 只有 1 条 | `<string` 计数 = 1 | ✓ |
| `CalendarHeatMap.kt` 181 行 | 180 行 | ✓（同上口径差） |
| 旧包名 schemas 25.9 KB | 25,959 B | ✓ |
| `dailywork.zip` 116 MB | 115,939,070 B | ✓ |
| `QA复核与修复方案.md` 318 行 / errors log 3.2 KB | 318 行 / 3,233 B | ✓ |
| 长函数行数（10 个） | 实测 501/425/398/342/318/205/199/180/172/152 vs 报告 502/432/399/341/318/211/200/181/181/151 | ✓ 大体吻合，个别差几行 |

**这份报告的事实层做得比大多数 AI 评估扎实——它是真的做了符号级扫描。** 下面的批评针对的是「怎么落地」这部分。

---

## 二、逐方向判定

### A1 删热力图死链 —— ✅ 结论成立，且比报告说的更确凿

`CalendarHeatMap` → `heatMapProgress` → `getHeatMapProgress` → `DayProgress` 四者互构成环，外部零引用：不仅是工作区，**在 HEAD（4c6dba3）也只有一个自引用**，我又把仓库全部 9 个提交扫了一遍：

```
4c6dba3 / 8eed251 / e17e7df / f0e8e54 / b77296d / b45b585 / a9e8603 → 仅自身或其 AndroidManifest
fdaf0e1 → 多出的一处在网页版已删除的 src/services/androidSources.ts（源码索引，非调用）
```

即：**这个组件从来没有被真正调用过**，不是某次重构掉落的活功能。README 第 14 行「历史热力图与流：35 天日历热力图」从头到尾是句假话。

> 判定：**可直接删**。约 220 行，风险确为零。这是我唯一完全同意报告原判的一项。

### A2 删 navigation-compose —— ✅ 成立，收益被低估

源码确实零引用（`MainScreen` 用的是 material3 的 `NavigationBar` + 整数 index，与 navigation-compose 无关）。
但报告漏了最关键的一点：**本项目 `isMinifyEnabled=false`，未用的库照样打进 dex**。我解析了 APK：

```
classes2.dex 内 androidx/navigation/ 类 = 296 个（零引用）
同 dex 内 coil/ = 322，okhttp3/ = 319，androidx/room/ = 238
```

删依赖 → dex 实打实变小（缺点字节数，但白赚）。**顺带发现：`okhttp3` 已经通过 coil 2.7.0 传递依赖进了 APK（319 个类 + `publicsuffixes.gz`），所以 `RemoteQuoteSource` 将来想换 OkHttp 不会增加体积**——项目记忆里「离线不能引 OkHttp」那条的前提其实已经部分失效了，这是报告没看到的隐藏机会。

### A3 清陈旧产物 —— ⚠️ 大部分可删，但 zip 不能随手删

| 项 | 判定 |
|---|---|
| 旧包名 schemas `{1,2,3}.json` | ✅ 可删。⚠️ 但只删 `com.example.habittracker.*`，**`io.github.*/3.json` 必须留**（它是 3→4 迁移的基线，也为将来引入 Room 迁移测试保留；当前离线环境装不了 `room-testing`，纸面基线更不能扔） |
| `QA复核与修复方案.md` | ✅ 可删（一次性文档，文末已有实施状态） |
| `.kotlin/errors/*.log` | ✅ 可删（已被 .gitignore 覆盖，纯垃圾） |
| `r1–r8.json` | ✅ 可删（全库无任何引用） |
| **`dailywork.zip`（116 MB）** | 🚨 **先问，别删**。它**不在 git 里**（`git ls-files` 查不到），删了无法从版本库恢复；内容是 `android/app` + `.gradle` 的整机快照，可能是你唯一的跨机备份。**这属于不可逆操作，我不会替你决定。** |

### B 文件归属修正 —— ✅ 方向对，一处数据错

- `HabitsManageScreen.kt` 只导出 `HabitManageSection`、被 `SettingsScreen.kt:144` 内嵌 ✓
- `Theme.kt` 27 行只有 `HabitTrackerTheme` ✓
- `HabitModels.kt` 42 + `SubTask.kt` 17 ✓
- ✗ `EmptyState.kt` 说「被 2 处使用」，实测 **3 处**：`TodayScreen` / `StandaloneCountersScreen` / `HabitsManageScreen`。57 行却三处复用，它不但不该并入别的文件，反而是这套代码里少见的优秀抽象——**这一条建议作废**。

### C 拆超长函数 —— ⚠️ 建议成立但场景判断失真

长函数清单属实（`AddEditHabitDialog` 501 行、`HabitCard` 425 行是真实痛点）。
但报告说「把 500 行表单切成 6 段，已有 `SubTaskEditor` 可复用同一模式」——低估了 Compose 特有的风险：

1. **拆涉及 `remember` / `mutableStateOf` / `collectAsState` 的块会改变状态宿主与重组范围**，这是行为变更，不是纯搬运；
2. 本项目刚踩过「返回值被丢弃 → 编译无警告、图标全变空白」的坑，**正是这类"看起来没动"的重构最容易翻车**；
3. 而本机**没有任何手段自动发现这种翻车**（见第三节）。

判定：可以做，但**不是现在、也不是一刀切 10 个**。建议先只动 `CalendarMonthView`（纯展示、无表单状态）验证流程，再谈别的。

### D1 颜色收敛 —— ⚠️ 定性夸大，执行方案要改

18 处属实，但报告说「这 18 个是绕过「主色不许和色板撞色」红线的后门」——**这句话混淆了两类东西**。我把每一处的使用上下文都读了一遍：

| 分类 | 值 | 处数 | 真实性质 |
|---|---|---|---|
| **真的踩红线** | `#10B981`（HabitsManageScreen:264，计数目标徽章） | 1 | 完成/进度语义写死了色板第一支，**违反项目自定规则**（应取 `colorScheme.primary`） |
| **色板同值但非红旗** | `#8B5CF6`×6、`#F59E0B`×2、`#3B82F6`、`#14B8A6` | 10 | 金刚区/徽章的**功能区分色**。语义正确，问题是「不上服从色板改动」这个 DRY 问题，不是撞色问题 |
| **压根不是色板色** | `#9CA3AF`×2、`#F97316`、`#EA580C`、`#6E5E4B` | 7 | 禁用灰 / 连续天数火焰橙 / 宣纸棕装饰色，与 ΔE 红线无关 |

报告给的药方「在 AppTheme 里定义 `UiAccent` 让 18 处全部引用」方向对，但要在长期真正生效，还得补两步它没说的：
- 这些颜色是**跨白天/夜间共用的固定值** —— 集中的真正收益应该是顺手给夜间模式做一次对比度核算（例如 `#6E5E4B` 在深底上的可读性），这才是隐藏债；
- 集中之后**把新常量纳入 `AppThemeTest`**（纯 Kotlin 常量，JVM 单测可直接引用，不需要 Color.parseColor），让红线真正有守卫。

### D2–D4 —— ✅ 基本属实，两处小修正

- 星期标签 4 份不同形态 ✓（`AddEditHabitDialog:91` / `CalendarMonthView:203` 内联 / `DayDetailDialog:68` / `HabitCard:606`）
- `notificationSettingsIntent` 两份实现 ✓；`calAccent()` / `detailAccent()` 纯冗余 ✓
- D4「116 条文案」：我按 `prop = "中文"` 形参口径测得 **103 条**，全量含中文字面量 549 条（含 `DailyQuotes` 128 条句子、`SampleData` 20 条、`AppTheme` 14 条个主题名）。**116 这个数量级可信，但偏低估**；而且真正该抽的和这 116 条不完全重合——周标签、色板名这类 **structured 文案压根不该进 strings.xml**，否则反而制造新的维护负担。

### E 主题样板压缩 —— ✅ 安全可行

`AppTheme.kt:49` 确实把 26 个参数在 dark/light 两分支各写一遍。改成 `(if (isDark) darkColorScheme() else lightColorScheme()).copy(...)` **语义等价**（未覆盖字段保持 M3 默认值，与现状一致），−26 行，后续加槽只改一处。这是整份报告里**性价比最高的一条**。

### F 注释叙事收敛 —— ✅ 属实（还漏了一处）

「`#10B981` 撞色故事写了 4 遍」属实（`HabitCard:100` / `CalendarMonthView:64` / `DayDetailDialog:63` / `CalendarHeatMap:163`），**外加 `TodayScreen.kt:100` 还有第 5 处**。
不过报告自己也说了这是「用可读性换行数」的唯一一项——我同意它的谨慎：**只收重复叙事，不要删单点说明**。这条建议你自己权衡，我认为优先级最低。

### G 真实重复 —— ✅ 属实，但也漏了几处

那 5 行前置计算确实在 `CalendarMonthView:532` 与 `DayDetailDialog:228` 重复。
我另跑了一遍「连续 5 行归一化」跨文件检测，报告没提的还有：
- **3 个 BroadcastReceiver 的 `getLongExtra → goAsync → CoroutineScope(IO).launch → finally.finish()` 样板**（`AlarmReceiver` / `CheckInActionReceiver` / `BootReceiver`），约 15 行 ×2 份重合，比它找到的那 10 行更实在；
- `LocalLifecycleOwner` 样板在 `AddEditHabitDialog` / `MainScreen` 重复。

「没有大面积复制粘贴」的大结论我认同，不必推翻。

---

## 三、报告最大的两个漏洞（可行性层面的硬伤）

### 🚨 漏洞 1：它假设了一份「干净的可回滚仓库」，但现状不是

```
HEAD = 4c6dba3 ("Big")
git status → 26 个文件 Modified / 1 个 Deleted
             TodayScreen +471  SettingsScreen +181  AddEditHabitDialog 重写 790 行
             HabitViewModel +87   build.gradle.kts / AndroidManifest 已改
             合计 +1907 / −1191，全部未提交
```

也就是说，**报告提议要拆的那些「超长函数」，恰恰活在还没进版本库的代码里**。在这种状态下动刀：

- 出事**不能用 `git revert` / `bisect` 定位**，只能靠记忆找回；
- 且你记忆里那条教训（不让 `git checkout -- .`）意味着恢复还得手挑路径，代价高。

> **真正的 P0 应该是：先把当前工作区跑一遍构建 + 单测、确认 71 条全绿，然后提交一次。** 这比删任何一行代码都重要。报告把「零风险」三个字给得太早了。

### 🚨 漏洞 2：它的「验证要求」在本机根本跑不了

报告第九节要求「删 `CalendarHeatMap.kt` 后必须跑视觉回归截图对比」。我去核了环境：

```
.gradle-home/caches/modules-2/files-2.1/ 下：
  androidx.compose.*  ✓（ui/ui-graphics/ui-text/ui-tooling-preview/ui-unit…）
  androidx.test       ✗ 不存在
  ui-test-junit4      ✗ 不存在
  robolectric         ✗ 不存在
且：无模拟器（历史约定）
```

→ **这句话在本地是不可执行的。** 它相当于给了一个自己也无法完成的验收条件。
可用替代（也是我建议的验收口径）：

1. `:app:assembleRelease :app:testDebugUnitTest` 必须 BUILD SUCCESSFUL；
2. **用 Python 解析 `test-results/*.xml` 数用例**：预期 **71 条全绿**（DailyQuotes 8 / QuotePolicy 9 / HabitIcons 4 / AppTheme 7 / CounterPeriod 17 / HabitSchedule 13 / Streak 13）；
3. 对 UI 改动建立**人工核对清单**（今日页 / 历史页 / 计数器页 / 管理中心 / 两个表单弹窗 × 白天+夜间），由你手动过一遍；
4. 改颜色后额外看一眼 `AppThemeTest` 的 ΔE 断言仍通过。

---

## 四、收益账：报告的总账口径混了四样东西，不能直接相加

报告第十节声称 main 行数 11,111 → ≈10,100（−9%）。我把各来源拆开重算：

| 来源 | 报告计入 | 真实性质 |
|---|---|---|
| A1 热力图死链 | 220 行 | ✅ 真源码 |
| A3 `QA复核与修复方案.md` 318 行 | — | 🚨 **P0 那句「≈540 行」就是 220 + 318 拼出来的，而 md 不属于 `app/src/main`** |
| A3 zip 116 MB | — | 体积，与行数无关 |
| E 主题样板 | 26 行 | ✅ 真源码 |
| D3 冗余工具 | ≈ 40 行 | ✅ 真源码 |
| C 拆函数 | — | 行数通常**微增**，文件峰值下降才是收益 |
| F 注释叙事 | 300–400 行 | ⚠️ 减的是**注释**，`实际代码 8,799` 这列基本不动 |

**重算：** 源码层面可稳定达成 ≈ **−290～300 行（约 11,060 → 10,760，−2.7%）**；若连 F 的注释一起收，总行数可到 ≈ 10,400（−6%）——但仍到不了它写的 10,100，除非把「拆函数会微增行数」这个常识也当成减项来算。

> 这不否定优化本身：**「文件峰值 840 → 400」和「18 处一致性后门归零」是真实收益，只是不能用行数来衡量**。别被 −9% 这个数字牵着走。

---

## 五、建议重排后的执行表

| 新优先级 | 动作 | 理由 | 验收 |
|---|---|---|---|
| **P0′** | `git add -A && git commit`（前提：先跑一次构建+71 条单测） | 现在没有任何可回滚基线 | BUILD SUCCESSFUL + XML 用例数 = 71 |
| **P1** | A1 删热力图链 + 同步删 README 第 14 行假描述 | 考古确证从未被调用 | 构建 + 71 条 + 人工过今日/历史页 |
| **P1** | A2 删 navigation-compose | dex 里白打包 296 类 | 构建 + APK dex 里 navigation 类数 = 0 |
| **P1** | E 主题样板 `.copy()` | 26 行、零行为变更、最高性价比 | 构建 + AppThemeTest ΔE 通过 |
| **P1** | A3 除 zip 外全部清理（旧 schemas / QA doc / errors log / r*.json） | 纯删除，无代码风险 | 构建通过 |
| **P2** | D3 合一 `notificationSettingsIntent`、删 `calAccent`/`detailAccent`、D2 星期合并 | 低风险机械重构 | 构建 + 71 条 |
| **P2** | D1 颜色：先修那 1 处真违规（`#10B981`→`colorScheme.primary`），再集中 10 处功能色到 `UiAccent`，7 处装饰色给夜间做对比度核算 | 分三类治理，别一刀切 | 人工视觉清单（含夜间）+ AppThemeTest 扩断言守卫新常量 |
| **P3** | C 拆分：先只做 `CalendarMonthView`（纯展示） | 建立「拆 → 验」流程信心 | 人工逐屏核对 |
| **P3** | B 改名/合并（**除 EmptyState 那条作废**）；F 注释收敛 | 收益主要是 AI 可读性 | 构建 + 71 条 |
| **待你确认** | `dailywork.zip` 116 MB | 未纳入 git，删了不可恢复 | 你点头我才动 |

---

## 六、给这份报告的评分

| 项 | 评分 | 备注 |
|---|---|---|
| 数据准确性 | ★★★★★ | 抽验 12 项，7 项精确吻合，其余仅差计数口径 |
| 结论可靠性 | ★★★★☆ | A1/A2 经得起考古级验证；D1 定性夸大、B 的 EmptyState 数据错 |
| 可执行性 | ★★☆☆☆ | 忽略未提交状态；给出本机无法执行的验收条件 |
| 收益估算诚实度 | ★★☆☆☆ | 540 行里 318 行是 markdown；−9% 是四样口径相加的产物 |

**总评：这是一份值得采纳的"体检报告"，但不是可以照着执行的"手术方案"。** 拿它定位问题是很好的输入；但它的执行顺序、风险分级和收益数字需要按上面这张表重写一遍才能用。
