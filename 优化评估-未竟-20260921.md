# 「未竟」Android 代码精简评估

> 评估对象：`dailywork/android`
> 评估时间：2026-09-21
> 方法：全量 Kotlin 源码符号扫描 + 跨文件引用分析 + 连续重复块检测；所有结论均给出文件与行号

---

## 一、基线数据

| 指标 | 数值 |
|---|---|
| Kotlin 文件 | 58（main 51 + test 7） |
| 总行数 | 12,343 |
| `app/src/main` 行数 | 11,111 |
| 其中注释 | 1,472 行（13%） |
| 其中空行 | 840 行（8%） |
| 实际代码 | ≈ 8,799 行 |
| 超过 700 行的文件 | 4 个 |
| 最大单文件 | `CalendarMonthView.kt` 840 行 |
| 界面文案硬编码在 Kotlin | 116 条 |
| `strings.xml` 条目 | 1 条（只有 `app_name`） |
| 非色板定义处的 `Color(0xFF…)` 字面量 | 18 处，散落 8 个文件 |
| **跨文件真实重复代码** | **仅 10 行**（1 处） |

**先说一个反直觉的结论**：这份代码**没有大面积复制粘贴**。用「连续 5 行归一化后完全一致」检测、排除 import 与注释后，全项目跨文件重复只有 10 行。之前粗测出的「20 处 / 18 处」绝大多数是 import 段和 Modifier 链的形态相似，不是真重复。

**所以优化方向不在「消除重复」，而在下面四件事：删死代码、修文件归属、拆超长函数、收敛散落常量。**

---

## 二、方向 A —— 纯删除死代码（零风险，立即可做）

### A1. 整条「热力图」功能已死

`ui/components/CalendarHeatMap.kt`（181 行）全项目**零引用**。顺着它往下查，整条链路都是死的：

| 符号 | 位置 | 状态 |
|---|---|---|
| `CalendarHeatMap`、`getHeatMapColor` | `CalendarHeatMap.kt`（整文件 181 行） | 零引用 |
| `heatMapProgress` | `HabitViewModel.kt:95` | 只被上面这个死组件消费 |
| `getHeatMapProgress` | `HabitRepository.kt:118` | 只被上面这个死字段消费 |
| `DayProgress` | `model/HabitModels.kt:36` | 只被上面这些死符号消费 |

已逐条 grep 确认：这条链上的每个符号只互相引用，**没有任何存活入口**。整条删除约 **220 行**。

> ⚠️ 注意：删掉后 `HabitRepository` 顶部的 `import model.DayProgress`、`HabitViewModel` 的 import 与 `.stateIn(...)` 块要一起清。`android/README.md` 里「历史热力图」的描述也要同步删。

### A2. 未使用的依赖

`androidx.navigation:navigation-compose`（`libs.versions.toml` 的 `navigationCompose = "2.8.4"`）在全部源码中**零引用** —— `MainScreen` 用的是 `index` 整数切 tab。可从 `build.gradle.kts` 与 `libs.versions.toml` 移除。

### A3. 陈旧产物

| 路径 | 体积 | 问题 |
|---|---|---|
| `app/schemas/com.example.habittracker.data.HabitDatabase/{1,2,3}.json` | 25.9 KB | **旧包名残留**。现行包名是 `io.github.molishadaze.weijing`，同名 3.json 在两边各存一份，比对迁移时会误导 |
| `android/QA复核与修复方案.md` | 318 行 | 一次性历史文档，文末已写「实施状态」，不属于工程代码 |
| `android/.kotlin/errors/errors-*.log` | 3.2 KB | 构建残骸（`.gitignore` 已忽略 `.kotlin/`，说明它本就不该在） |
| `dailywork/` 根目录 `dailywork.zip` | **116 MB** | 含 `android/app` 4131 项 + `.gradle` 192 项构建缓存的整包快照，任何一次工作区列表都会带上它 |
| `dailywork/` 根目录 `r1.json` … `r8.json` | 3.3 KB | 格言接口的 8 条样例响应，调试残留 |

---

## 三、方向 B —— 文件归属错位（最影响「AI 定位」的一项）

这一项不减行数，但对「AI 读取负担」收益最大 —— 因为**文件名指向了错误的东西**，AI 会去错的地方找代码。

| 文件 | 现状 | 建议 |
|---|---|---|
| `ui/screens/HabitsManageScreen.kt`（400 行） | 文件名像是「习惯管理页」，但里面**唯一对外符号是 `HabitManageSection`**，由 `SettingsScreen.kt:144` 内嵌调用；不存在 `HabitsManageScreen()` 这个页面 | 改名 `ui/components/HabitManageSection.kt`，或直接并入 `SettingsScreen.kt` |
| `ui/theme/Theme.kt`（27 行） | 只放一个 `HabitTrackerTheme`，与 `AppTheme.kt` 名字高度相似，检索时反复走错 | 并入 `AppTheme.kt` |
| `model/HabitModels.kt`（42 行）+ `model/SubTask.kt`（17 行） | 两个文件共 3 个 data class | 合并为 `model/Models.kt` |
| `ui/components/EmptyState.kt` | 仅 57 行，被 2 处使用 | 可并入邻近组件文件 |

按此整理，文件数可从 51 → **47**，`ui/screens/` 与 `ui/components/` 的职责边界也会变清晰。

---

## 四、方向 C —— 超长函数（AI 单次读取的真正负担）

| 函数 | 文件 | 行数 |
|---|---|---|
| `AddEditHabitDialog()` | `AddEditHabitDialog.kt:111` | **502** |
| `HabitCard()` | `HabitCard.kt:65` | **432** |
| `CalendarMonthView()` | `CalendarMonthView.kt:98` | **399** |
| `DayDetailPlanCard()` | `DayDetailDialog.kt:216` | **341** |
| `CalendarDayPlanRow()` | `CalendarMonthView.kt:523` | **318** |
| `CounterCard()` | `StandaloneCountersScreen.kt:280` | 211 |
| `StandaloneCountersScreen()` | 同文件 :80 | 200 |
| `AddEditCounterDialog()` | `AddEditCounterDialog.kt:36` | 181 |
| `HabitManageCard()` | `HabitsManageScreen.kt:204` | 181 |
| `MainScreen()` | `MainScreen.kt` | 151 |

**拆分建议（不改变行为，只把大函数切成有名字的小函数）：**

- `AddEditHabitDialog` → 切为「基本信息 / 排期设置 / 计数器目标 / 子任务编辑 / 提醒 / 底部按钮」6 段。文件已有 `SubTaskEditor`(142 行)、`DatePickerButton` 可复用同一模式。
- `CalendarMonthView` → 切为「月头导航 / 日历网格 / 图例 / 当日清单」4 段，与文件里已有的 `LegendDot`、`CalendarDayPlanRow` 对齐。
- `HabitCard` / `DayDetailPlanCard` / `CalendarDayPlanRow` → 三者在结构上都是「习惯行 + 计数 + 子任务」，可共用一套行内渲染组件。

**收益不是行数减少（通常会微增），而是把单文件峰值从 840 行压到 400 行左右** —— 这直接决定 AI 读一个文件要花多少上下文。

---

## 五、方向 D —— 散落常量收敛（一致性风险）

### D1. 颜色：违反项目自己立下的规则

项目约定写着「颜色唯一真相来源 = `AppTheme`」，但实际有 **18 处** 硬编码 hex 落在 8 个文件里（`AppTheme.kt` 内的 150 处是合法色板定义，不计）：

| 值 | 出现次数 | 位置 |
|---|---|---|
| `0xFF8B5CF6` 紫 | 6 处，各起一个名字 | `PLAN_PURPLE`(AddEditHabitDialog:89)、`CAL_PURPLE`(CalendarMonthView:59)、`DETAIL_PURPLE`(DayDetailDialog:59)、`COUNTER_PURPLE`(StandaloneCountersScreen:71)、`COUNTER_TAB_COLOR`(MainScreen:55)、`SettingsScreen:279/289`、`HabitsManageScreen:258` |
| `0xFFF59E0B` 琥珀 | 2 处 | `CAL_AMBER`(CalendarMonthView:58)、`SettingsScreen:238` |
| `0xFF3B82F6` 蓝 | 1 处 | `SettingsScreen:237` |
| `0xFF10B981` 翡翠 | 1 处 | `HabitsManageScreen:264` |
| `0xFF14B8A6` 青碧 | 1 处 | `SettingsScreen:224` |

**风险是实打实的**：这些值恰好就是「习惯色板 / 计数器色板」里的颜色。一旦调整色板，这 18 处不会跟着变，而 `AppThemeTest` 的 ΔE 断言也覆盖不到它们 —— 也就是说，**项目最在意的那条「主色不许和色板撞色」的红线，存在 18 个绕过它的后门**。

建议：在 `AppTheme.kt` 里显式定义一组「UI 强调色」（如 `PlanPalette.Purple` / `UiAccent.Amber`），全部改为引用。

### D2. 星期标签重复 4 份不同形态

| 位置 | 形态 |
|---|---|
| `AddEditHabitDialog.kt:92` | `WEEKDAY_LABELS` = `listOf(1 to "一", …)` |
| `CalendarMonthView.kt:203` | 函数内联 `listOf("周一", "周二", …)` |
| `DayDetailDialog.kt:68` | `DETAIL_WEEKDAYS` = `listOf("周一", …)` |
| `HabitCard.kt:606` | `WEEKDAY_CN` = `mapOf(1 to "一", …)` |

四份数据、三种形态。收敛到一个 `util/Weekdays.kt`。

### D3. 其他重复工具

- `notificationSettingsIntent(context)` 存在**两份不同实现**：`AddEditHabitDialog.kt:808`（internal，action 写在构造函数里）与 `SettingsScreen.kt:704`（private，空 Intent 后赋 `action`）。语义等价，应合一。
- `calAccent()`（`CalendarMonthView.kt:69`）与 `detailAccent()`（`DayDetailDialog.kt:66`）两个私有 `@Composable`，函数体都是 `MaterialTheme.colorScheme.primary`，纯冗余。

### D4. 界面文案未抽取

116 条中文界面串直接写在 Kotlin 里，`strings.xml` 只有 `app_name` 一条。抽取的收益不只是 i18n：**文案集中在 xml 后，AI 改 UI 时不必再读一遍所有 Composable 去找句子**。

---

## 六、方向 E —— 主题样板压缩

`AppTheme.kt`（366 行）里：

- `ThemePalette` 26 个字段 × 6 套配色 ≈ **156 行**（属于数据，不建议压）
- `toColorScheme()` 把同样的 26 个参数在 `darkColorScheme` / `lightColorScheme` 两个分支里**各写一遍** ≈ **52 行**

第二项可以合并成：

```kotlin
fun ThemePalette.toColorScheme(): ColorScheme =
    (if (isDark) darkColorScheme() else lightColorScheme()).copy(
        primary = primary, onPrimary = onPrimary, /* …26 个参数只写一遍… */
    )
```

省约 **26 行**，且以后新增语义槽只用改一处。

---

## 七、方向 F —— 注释里的「历史事故」叙事收敛

1,472 行注释质量很高（写的是「为什么」而非「是什么」），但有一类明显冗余：**同一个「`#10B981` 与计划色板撞色」的故事被复述了 4 遍** ——

`HabitCard.kt:100`、`CalendarMonthView.kt:64`、`DayDetailDialog.kt:63`、`CalendarHeatMap.kt:163`（另见 `AppTheme.kt:87`）。

源码里各留一行指针（「撞色原因见 AppTheme.kt 顶部」），把完整叙事收进 `README.md`，可压缩 **300–400 行**。

> 注意：这是唯一一项「用可读性换取行数」的取舍。注释本身是好东西，建议只收敛**重复叙事**，不要删单点说明。

---

## 八、方向 G —— 真实重复（量小，但确实存在）

唯一的跨文件重复块：`CalendarMonthView.kt:532` 与 `DayDetailDialog.kt:228` 共享同一段前置计算：

```kotlin
val isDone = HabitSchedule.isCompleted(habit, checkIn)
val count = HabitSchedule.currentCount(checkIn)
val target = HabitSchedule.effectiveTarget(habit)
val hasSubTasks = habit.subTaskList.isNotEmpty()
val completedIds = checkIn?.completedSubTaskIdList.orEmpty()
```

可抽成 `rememberDayRowState(habit, checkIn)` 供两处共用。

---

## 九、执行顺序与风险

| 优先级 | 内容 | 风险 | 预计收益 |
|---|---|---|---|
| **P0** | A1 删热力图死链、A2 删 navigation 依赖、A3 清陈旧产物 | 零（纯删除） | ≈ **540 行** + 116 MB |
| **P1** | B 文件改名/合并、D1-D3 常量收敛、D4 文案抽取 | 低（不改逻辑） | 文件 51→47；消除 18 处一致性后门 |
| **P2** | C 超长函数拆分、E 主题样板、F 注释收敛 | 中（需回归验证） | 单文件峰值 840→400；≈ 400 行 |

**验证要求（照项目既有规矩来）：**
1. 每步之后跑 `:app:assembleRelease :app:testDebugUnitTest`，并用 Python 解析 `test-results` XML 核对用例数 —— 「BUILD SUCCESSFUL」不等于测试跑了。
2. 删 `CalendarHeatMap.kt` 后必须跑 `HabitIconsTest` 之外的**视觉回归**（今日页 / 历史页 / 计数器页截图对比），因为「编译通过 ≠ 画得出来」是本项目踩过的坑。
3. 改 `AppTheme.kt` 后 `AppThemeTest` 的 ΔE 断言必须仍然通过。
4. 删文件用 `rm`，**不要用 `git rm`**（曾导致整个目录连同其他文件一起消失）。

---

## 十、总账

| 项目 | 现状 | 目标 |
|---|---|---|
| main 代码行数 | 11,111 | ≈ 10,100（-9%） |
| 实际代码行 | 8,799 | ≈ 8,100 |
| 文件数 | 51 | ≈ 47 |
| 最大单文件 | 840 行 | ≈ 400 行 |
| 死代码 | 220 行 | 0 |
| 硬编码界面文案 | 116 条 | ≈ 0 |
| 绕过 AppTheme 的颜色 | 18 处 | 0 |
| 工作区体积 | 116 MB 冗余 | 0 |
